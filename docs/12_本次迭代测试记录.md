# 12 本次迭代测试记录

## 本次修改

1. 项目重命名为：

```text
mmd-avatar-web-viewer
```

2. `package.json` 已更新：

```text
name: mmd-avatar-web-viewer
version: 0.12.0
```

3. 新增启动前自动扫描：

```text
scripts/scan_public_assets.py
```

扫描目录：

```text
public/models
public/motions
public/poses
```

输出：

```text
public/resource_manifest.json
```

4. 前端新增动态下拉列表：

```text
角色列表：characterSelect
VMD 动作列表：motionSelect
VPD 姿态列表：poseSelect
```

5. Manifest 与扫描资源合并：

```text
public/models/avatar_manifest.json
public/motions/vmd_manifest.json
public/resource_manifest.json
```

manifest 中声明的资源使用 manifest 显示名和来源说明；额外扫描到的合法资源也会动态加载。

6. `dist/` 支持静态扫描：

```bash
npm run serve:dist
```

会先扫描 `dist/models`、`dist/motions`、`dist/poses`，再启动静态服务。

## 检查结果

已执行：

```bash
bash scripts/auto_check_all.sh
```

结果：

```text
PASS: required checks passed
```

新增检查：

```text
scripts/check_resource_scanning.py
```

验证内容：

```text
[PASS] public/resource_manifest.json 可生成
[PASS] hiying 仍为默认角色
[PASS] qianyeBlade 保留在角色列表
[PASS] breath / idle / wave / jump / peek / shotenTime / goodTea / lovermaxHipSway 均进入资源索引
[PASS] 临时新增合法 VMD 不改配置也能被扫描进 manifest
[PASS] 前端 smoke test 确认角色、VMD、VPD 下拉列表可用
```

名称级动作适配仍通过：绯英与千冶·刃均具备当前关键 VMD 动作所需 bone/morph 名称。该检查能证明 VMD 能按名称驱动角色，但长动作是否穿模、脚滑或镜头是否好看，仍需要浏览器肉眼预览。
